# 📊 PageSpeed Insights Monitoring Script


# 📊 PageSpeed Insights Monitoring Script - Обновления

## 🆕 История изменений

---

## Версия 5.2 (Февраль 2026)

### 🎨 Улучшенная цветовая схема

**Проблема:** Градиент данных не отображался из-за конфликта с фоновыми цветами.

**Исправление:**
- Удалены фоновые цвета, которые перезаписывали градиент в колонке данных
- Фоновые цвета теперь применяются только к колонке меток (колонка A)
- Цветовой градиент теперь корректно отображается на всех ячейках с данными

**Новые яркие цвета (Material Design):**

| Статус | Старый цвет | Новый цвет | Hex |
|--------|-------------|------------|-----|
| 🟢 GOOD | `#34A853` | `#00C853` | Яркий изумрудно-зелёный |
| 🟠 NEEDS_IMPROVEMENT | `#FBBC04` | `#FF9100` | Яркий оранжевый |
| 🔴 POOR | `#EA4335` | `#FF1744` | Яркий красный |

**Преимущества новых цветов:**
- ✅ Больший контраст между статусами
- ✅ Более насыщенные и заметные оттенки
- ✅ Лучшая читаемость на любых экранах
- ✅ Соответствие стандартам Material Design
- ✅ Улучшенная доступность для людей с нарушениями зрения

### 🔍 Расширенная система диагностики

**Новая функция: `testColorScheme()`**

Быстрое тестирование цветовой схемы прямо в таблице.

**Доступ:** Меню → `📊 PageSpeed Monitoring` → `🧪 Тест цветовой схемы`

**Что делает:**
- Создаёт тестовую строку с тремя цветными ячейками
- Показывает все три цвета градиента (GOOD, NEEDS_IMPROVEMENT, POOR)
- Выводит текущую конфигурацию цветов в логах
- Позволяет мгновенно проверить, работает ли градиент

**Детальное логирование:**

Все функции применения цветов теперь подробно логируются:

```javascript
━━━ ПРИМЕНЕНИЕ ЦВЕТА ━━━
Ячейка: [5, 2]
Метрика: LCP
Значение: 1.5
Выбранный цвет: #00C853
✅ Цвет успешно применён
```

**Автоматическая проверка конфигурации:**

При запуске `collectPageSpeedData()` автоматически выводится:
- Текущие значения `CONFIG.COLORS`
- Пороговые значения `CONFIG.THRESHOLDS`
- Помогает выявить проблемы до начала сбора данных

### 📝 Улучшенное логирование статусов

Цвета логов также обновлены на более яркие:
- SUCCESS/OK: `#00C853` (яркий зелёный)
- ERROR/FAIL: `#FF1744` (яркий красный)
- WARNING: `#FF9100` (яркий оранжевый)
- INFO: `#2979FF` (яркий синий)

---

## Версия 5.0 (Февраль 2026)

### 🌐 Группировка URL по доменам

**Новая структура конфигурации:**

```javascript
DOMAINS: {
  'example.com': [
    '/',
    '/catalog',
    '/product/item-name/',
  ],
  'another-site.com': [
    '/',
    '/about',
  ]
}
```

**Преимущества:**
- Логическая группировка URL по сайтам
- Визуальное разделение доменов в таблице
- Цветные заголовки для каждого домена
- Проще добавлять и управлять множеством сайтов

### 📐 Компактная таблица (9 строк вместо 18)

**Новая структура блока URL:**

```
┌─────────────────────┐
│ 🌐 example.com      │ ← Заголовок домена (цветной)
├─────────────────────┤
│ /catalog            │ ← Путь (URL баннер)
│ 📱 LCP (сек)        │
│ 📱 INP (мс)         │
│ 📱 CLS              │
│ 📱 Score            │
│ 🖥️ LCP (сек)        │
│ 🖥️ INP (мс)         │
│ 🖥️ CLS              │
│ 🖥️ Score            │
└─────────────────────┘
```

**Изменения:**
- **Было:** 18 строк на URL (отдельные заголовки для Mobile и Desktop)
- **Стало:** 9 строк на URL (Mobile и Desktop в одном блоке)
- **Результат:** В 2 раза компактнее, легче читать и анализировать

### 🎨 Цветные заголовки доменов

Каждый домен получает уникальный яркий цвет:

| Порядок | Цвет | Hex | Название |
|---------|------|-----|----------|
| 1 | 🔵 Синий | `#2979FF` | Material Blue A400 |
| 2 | 🟢 Зелёный | `#00C853` | Material Green A700 |
| 3 | 🟣 Фиолетовый | `#D500F9` | Material Purple A400 |
| 4 | 🟠 Оранжевый | `#FF6D00` | Material Orange A700 |
| 5 | 🔴 Красный | `#FF1744` | Material Red A400 |
| 6 | 🔷 Бирюзовый | `#00E5FF` | Material Cyan A400 |

**Настройка:** `CONFIG.DOMAIN_COLORS`

Цвета автоматически повторяются по кругу, если доменов больше 6.

### 🔄 Улучшенная навигация меню

**Структурированное подменю для одиночных URL:**

```
📊 PageSpeed Monitoring
├─ 🔄 Собрать данные для ВСЕХ URL
├─ 📍 Собрать для одного URL
│  ├─ 🌐 example.com
│  │  ├─ / (главная)
│  │  ├─ /catalog
│  │  └─ /product/item/
│  └─ 🌐 another-site.com
│     ├─ / (главная)
│     └─ /about
└─ ...
```

---

## 📖 Обновлённое использование

### Конфигурация доменов

**1. Добавление нового домена:**

```javascript
DOMAINS: {
  'example.com': [
    '/',
    '/page1'
  ],
  'new-site.com': [  // ← Новый домен
    '/',
    '/about',
    '/contact'
  ]
}
```

**2. Добавление URL в существующий домен:**

```javascript
'example.com': [
  '/',
  '/page1',
  '/page2',  // ← Новый URL
]
```

### Функция тестирования

**Запуск теста:**
1. Откройте Google Таблицу
2. Меню → `📊 PageSpeed Monitoring` → `🧪 Тест цветовой схемы`
3. Проверьте созданную строку с тремя цветами

**Интерпретация результатов:**

✅ **Успешно:** Видны три яркие ячейки (зелёная, оранжевая, красная)  
❌ **Проблема:** Ячейки белые или бледные → проверьте логи выполнения

### Просмотр детальных логов

**Через редактор скриптов:**
1. `Расширения` → `Apps Script`
2. Нажмите `Ctrl+Enter` (или `Cmd+Enter` на Mac)
3. Выберите функцию (например, `testColorScheme`)
4. Нажмите "Выполнить"
5. Смотрите логи в нижней панели

**Через журнал выполнений:**
1. `Расширения` → `Apps Script`
2. Левое меню → `Выполнения` (иконка часов)
3. Выберите последнее выполнение
4. Изучите детальный лог

---

## 🔧 Миграция с версии 4.x

### Обновление конфигурации

**Старая структура (v4.x):**
```javascript
URLS: [
  'https://example.com',
  'https://example.com/page1',
  'https://other-site.com'
]
```

**Новая структура (v5.x):**
```javascript
DOMAINS: {
  'example.com': [
    '/',
    '/page1'
  ],
  'other-site.com': [
    '/'
  ]
}
```

### Шаги миграции

1. **Обновите код:** Замените `main.js` на версию 5.2
2. **Конвертируйте URL:**
   - Из `https://example.com/page` → `'example.com': ['/page']`
   - Группируйте по доменам
3. **Пересоздайте структуру:** Меню → `🔨 Пересоздать структуру листа`
4. **Запустите сбор:** Меню → `🔄 Собрать данные для ВСЕХ URL`

> ⚠️ **Важно:** После миграции старые данные сохранятся, но будут созданы новые блоки с компактной структурой.

---

## 🆕 Новые функции меню (v5.2)

| Пункт меню | Описание | Версия |
|-----------|----------|--------|
| 🧪 Тест цветовой схемы | Быстрая проверка градиента | 5.2 |
| 📍 Собрать для одного URL | Структурировано по доменам | 5.0 |

---

## 📊 Сравнение версий

### Структура таблицы

| Параметр | v4.4 | v5.2 |
|----------|------|------|
| Строк на URL | 18 | 9 |
| Группировка | Нет | По доменам |
| Заголовки доменов | Нет | Цветные |
| Цвета градиента | Стандартные | Яркие (Material) |

### Конфигурация

| Параметр | v4.4 | v5.2 |
|----------|------|------|
| Структура URL | Массив `URLS` | Объект `DOMAINS` |
| Цвета заголовков | Нет | `DOMAIN_COLORS` |
| Система диагностики | Нет | `testColorScheme()` |

---

## 🐛 Известные исправления

### v5.2: Исправлена проблема с градиентом

**Симптомы:**
- Цвета данных бледные (#F0F4FF, #FFF8F0)
- Тест цветов показывает яркие цвета, но данные — бледные

**Причина:** Фоновые цвета Mobile/Desktop блоков перезаписывали градиент

**Решение:** Фоновые цвета применяются только к колонке меток (A), не к данным

---

## 📝 Рекомендации

### Оптимальное количество URL

| Режим запуска | Рекомендуемое количество URL |
|---------------|------------------------------|
| Ручной сбор | 5-7 URL (лимит 6 минут) |
| Триггеры | До 20-30 URL (лимит 30 минут) |

### Использование цветов доменов

**Для лучшей визуализации:**
- Используйте разные цвета для конкурирующих сайтов
- Группируйте схожие проекты рядом
- Настройте порядок цветов в `DOMAIN_COLORS`

### Диагностика проблем

**Если градиент не работает:**
1. Запустите `🧪 Тест цветовой схемы`
2. Проверьте логи выполнения
3. Включите `ENABLE_LOGGING: true`
4. Пересоздайте структуру листа

---

## 🔗 Связанные файлы

- `COLOR_DIAGNOSTICS.md` - Подробное руководство по диагностике цветов
- `PROBLEM_SOLVED.md` - Описание исправленной проблемы с градиентом
- `color_changes.html` - Визуализация изменений цветовой схемы

---

**Версия документации:** 2.0  
**Дата обновления:** Февраль 2026  
**Совместимость:** Google Apps Script, Google Sheets


**Версия:** 4.4  
**Назначение:** Автоматический мониторинг производительности веб-страниц через Google PageSpeed Insights API

---

## 🎯 Основные возможности

- Автоматический сбор метрик производительности (LCP, INP, CLS, Performance Score)
- Мониторинг Mobile и Desktop версий
- Цветовая индикация метрик (Good/Needs Improvement/Poor)
- Автоматические повторные попытки при ошибках
- Поддержка Lab данных как fallback при отсутствии Field данных
- Настройка триггеров для автоматического сбора
- Логирование (опционально)

---

## ⚙️ Конфигурация

### Основные параметры

```javascript
const CONFIG = {
  API_KEY: 'YOUR_GOOGLE_API_KEY',
  
  URLS: [
    'https://example.com',
    'https://example.com/page1',
    'https://example.com/page2'
  ],
  
  SHEET_NAME: 'PageSpeed Dashboard',
  LOG_SHEET_NAME: 'Логи',
  ENABLE_LOGGING: false,
  
  MAX_RETRIES: 2,
  RETRY_DELAY: 3,
  REQUEST_DELAY: 2,
  MAX_EXECUTION_TIME: 300,
  ROWS_PER_URL: 18
}
```

### Пороговые значения метрик

| Метрика | Good | Needs Improvement | Poor |
|---------|------|-------------------|------|
| LCP (сек) | ≤ 2.5 | ≤ 4.0 | > 4.0 |
| INP (мс) | ≤ 200 | ≤ 500 | > 500 |
| CLS | ≤ 0.1 | ≤ 0.25 | > 0.25 |
| Performance Score | ≥ 90 | ≥ 50 | < 50 |

---

## 🚀 Установка

**1.** Откройте Google Sheets → `Расширения → Apps Script`

**2.** Вставьте код скрипта

**3.** Получите API ключ Google PageSpeed Insights:
   - Перейдите в [Google Cloud Console](https://console.cloud.google.com)
   - Создайте проект или выберите существующий
   - Включите PageSpeed Insights API
   - Создайте API ключ в разделе "Credentials"

**4.** Настройте `CONFIG.API_KEY` и `CONFIG.URLS`

**5.** Сохраните скрипт (Ctrl+S)

**6.** Обновите страницу Google Sheets — появится меню `📊 PageSpeed Monitoring`

---

## 📖 Использование

### Ручной сбор данных

**Для всех URL:** Меню → `🔄 Собрать данные для ВСЕХ URL`  
**Для одного URL:** Меню → `📍 Собрать для одного URL` → выберите URL

### Автоматический сбор (триггеры)

| Опция | Описание |
|-------|----------|
| ⏰ Ежедневный сбор | Запуск каждый день в 9:00 |
| 📅 Еженедельный сбор | Запуск каждый понедельник в 9:00 |
| ⚙️ Свое время запуска | Выбор произвольного времени (0-23) |

> **Преимущество триггеров:** Нет лимита времени выполнения в 6 минут (как при ручном запуске)

### Управление

- `📋 Показать список URL` — просмотр всех отслеживаемых URL
- `🔨 Пересоздать структуру листа` — полное пересоздание таблицы
- `👁️ Активные триггеры` — просмотр настроенных триггеров
- `🛑 Отключить автосбор` — удаление всех триггеров
- `🕐 Показать часовой пояс` — информация о текущем часовом поясе
- `🌍 Изменить часовой пояс` — настройка часового пояса

---

## 📊 Структура данных

Для каждого URL создается блок из 18 строк:

```
URL (строка 1)
─────────────
📱 MOBILE
  LCP (сек)
  INP (мс)
  CLS
  Performance Score

🖥️ DESKTOP
  LCP (сек)
  INP (мс)
  CLS
  Performance Score
```

> Каждая колонка = новая дата сбора данных  
> Цветовая индикация: 🟢 Good | 🟡 Needs Improvement | 🔴 Poor

---

## 🔧 Логирование

Для включения логирования установите `CONFIG.ENABLE_LOGGING = true`

### Функции логирования

- `📜 Посмотреть логи` — открыть лист с логами
- `🗑️ Очистить логи` — удалить все записи

---

## ⚠️ Ограничения и рекомендации

### Лимиты времени

- **Ручной запуск:** максимум 6 минут
- **Триггеры:** максимум 30 минут
- **Рекомендуется:** не более 5-7 URL при ручном запуске

### Лимиты API

- Google PageSpeed Insights API имеет дневные лимиты запросов
- При превышении возникает ошибка 429 (Too Many Requests)
- Рекомендуется: установить задержку между запросами (REQUEST_DELAY)

---

## 🐛 Обработка ошибок

### Типичные ошибки и решения

| Ошибка | Причина | Решение |
|--------|---------|---------|
| NO_FCP / NO_LCP | Страница не загрузилась или требует авторизации | Скрипт автоматически использует Lab данные как fallback |
| 429 Too Many Requests | Превышен лимит API | Подождите 5-10 минут, используйте триггеры |
| Exceeded maximum execution time | Превышен лимит времени | Уменьшите количество URL или используйте триггеры |
| 400 Bad Request | Неверный URL или API ключ | Проверьте корректность данных в CONFIG |

---

## 📝 Техническая информация

### Основные функции

| Функция | Описание |
|---------|----------|
| `collectPageSpeedData()` | Сбор данных для всех URL |
| `collectDataForSingleUrl(index)` | Сбор данных для одного URL по индексу |
| `fetchDataWithRetry(url, strategy)` | Запрос к API с повторными попытками |
| `saveDataToUrlBlock()` | Сохранение данных в таблицу |
| `setupDailyTrigger()` | Настройка ежедневного триггера |

### Алгоритм работы

1. Получение или создание листа Dashboard
2. Определение колонки для текущей даты
3. Для каждого URL:
   - Запрос Mobile данных через API
   - Задержка (REQUEST_DELAY)
   - Запрос Desktop данных через API
   - Сохранение в таблицу с форматированием и цветовой индикацией
4. Отображение итогового отчета

---

## 🔗 Полезные ссылки

- [Google PageSpeed Insights API Documentation](https://developers.google.com/speed/docs/insights/v5/get-started)
- [Web Vitals Documentation](https://web.dev/vitals/)
- [Google Cloud Console](https://console.cloud.google.com)

---

**Версия документации:** 1.0  
**Последнее обновление:** Январь 2026# gsc_api_google_sheet
# gsc_api_google_sheet
